[package]
name = "qnc-service-contracts"
version = "0.5.1"
edition = "2021"
description = "Neutral QNC service contracts for media, AI, search, and remote adapters"

[dependencies]
async-trait = "0.1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
